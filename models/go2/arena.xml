<mujoco model="go2_arena">
  <include file="go2.xml"/>

  <statistic center="0 0 0.3" extent="4"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.1 0.15 0.2 1"/>
    <global azimuth="120" elevation="-30"/>
  </visual>

  <asset>
    <texture type="2d" name="floor_tex" builtin="checker"
      rgb1="0.35 0.55 0.35" rgb2="0.28 0.45 0.28" width="256" height="256"/>
    <material name="floor_mat" texture="floor_tex" texrepeat="5 5" reflectance="0.1"/>
    <material name="wall_mat" rgba="0.55 0.55 0.65 1"/>
    <material name="oni_mat"  rgba="0.95 0.12 0.12 1"/>
  </asset>

  <worldbody>
    <light name="main_light" pos="0 0 5" dir="0 0 -1" directional="true"
      diffuse="0.8 0.8 0.8" specular="0.1 0.1 0.1"/>
    <light name="fill_light" pos="0 0 3" dir="0 1 -1" directional="true"
      diffuse="0.3 0.3 0.35" specular="0"/>

    <!-- 5m×5m フロア（±2.5m） -->
    <geom name="floor" type="plane" size="2.5 2.5 0.1" material="floor_mat"
      contype="1" conaffinity="1" friction="0.8 0.1 0.1"/>

    <!-- 壁（厚み 0.1m、高さ 1.0m） -->
    <!-- 北壁 y=+2.5 -->
    <geom name="wall_n" type="box" pos="0  2.55 0.5" size="2.6 0.05 0.5"
      material="wall_mat" contype="1" conaffinity="1"/>
    <!-- 南壁 y=-2.5 -->
    <geom name="wall_s" type="box" pos="0 -2.55 0.5" size="2.6 0.05 0.5"
      material="wall_mat" contype="1" conaffinity="1"/>
    <!-- 東壁 x=+2.5 -->
    <geom name="wall_e" type="box" pos=" 2.55 0 0.5" size="0.05 2.6 0.5"
      material="wall_mat" contype="1" conaffinity="1"/>
    <!-- 西壁 x=-2.5 -->
    <geom name="wall_w" type="box" pos="-2.55 0 0.5" size="0.05 2.6 0.5"
      material="wall_mat" contype="1" conaffinity="1"/>

    <!-- 俯瞰カメラ（2体が収まる高さ: 9m） -->
    <camera name="top_view" pos="0 0 9" euler="0 0 0" fovy="60"/>

    <!-- 斜めカメラ（確認用） -->
    <camera name="side_view" pos="6 -4 4" euler="55 0 45" fovy="50"/>

    <!-- 鬼（赤い球）: pos=(0,0,0.3) を原点に slide 関節で移動
         qpos[19]=oni_x, qpos[20]=oni_y で位置管理 -->
    <body name="oni" pos="0 0 0.3">
      <joint name="oni_x" type="slide" axis="1 0 0"
        limited="true" range="-2.4 2.4" damping="100" frictionloss="0"/>
      <joint name="oni_y" type="slide" axis="0 1 0"
        limited="true" range="-2.4 2.4" damping="100" frictionloss="0"/>
      <!-- 視覚的な球（衝突なし: contype=0） -->
      <geom name="oni_body" type="sphere" size="0.28" material="oni_mat"
        contype="0" conaffinity="0" mass="0.001"/>
      <!-- 鬼の方向インジケーター（目のような突起） -->
      <geom name="oni_eye" type="sphere" size="0.08" pos="0.25 0 0.1"
        rgba="1 1 1 1" contype="0" conaffinity="0" mass="0"/>
    </body>
  </worldbody>
</mujoco>
